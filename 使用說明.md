# 🗽 NYC Travel Assistant — 使用說明

> 為華人遊客打造的紐約智慧旅遊助手，結合 Google Places 資料、線上編輯推薦與 AI 排序，提供個人化美食和景點推薦。

---

## 📋 目錄

1. [環境需求](#1-環境需求)
2. [安裝步驟](#2-安裝步驟)
3. [環境變數設定](#3-環境變數設定)
4. [啟動服務](#4-啟動服務)
5. [前端使用指南](#5-前端使用指南)
6. [API 說明](#6-api-說明)
7. [AI 模式 vs Raw 模式](#7-ai-模式-vs-raw-模式)
8. [專案結構](#8-專案結構)
9. [常見問題](#9-常見問題)

---

## 1. 環境需求

| 項目 | 版本 |
|------|------|
| Python | 3.10+ |
| pip | 最新版 |
| Ollama（本地 LLM） | 最新版（可選） |
| Azure OpenAI（雲端 LLM） | 可選 |

---

## 2. 安裝步驟

```bash
# 進入專案目錄
cd nyc-travel-assistant

# 建立 Python 虛擬環境
python3 -m venv .venv
source .venv/bin/activate

# 安裝依賴
pip install -r backend/requirements.txt
```

### 依賴清單

| 套件 | 用途 |
|------|------|
| `fastapi` | Web 框架 |
| `uvicorn` | ASGI 伺服器 |
| `httpx` | 非同步 HTTP 請求（呼叫 LLM） |
| `pydantic` | 資料驗證 |
| `duckduckgo-search` | 搜尋差評資訊 |
| `python-dotenv` | 環境變數載入 |

---

## 3. 環境變數設定

在 `backend/` 資料夾下建立 `.env` 檔案：

### 使用 Ollama（本地模型，免費）

```env
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
```

> 💡 如果你的電腦記憶體充足（≥16GB），推薦用 `qwen2.5:14b`，中文能力更強。

### 使用 Azure OpenAI（雲端模型）

```env
LLM_PROVIDER=azure
AZURE_API_KEY=你的金鑰
AZURE_ENDPOINT=你的端點URL
AZURE_API_VERSION=2025-01-01-preview
AZURE_DEPLOYMENT=gpt-5.2
```

> 如果不設定 `LLM_PROVIDER`，預設為 `azure`。

---

## 4. 啟動服務

### 4.1 啟動 Ollama（如果用本地模型）

```bash
# 安裝 Ollama（如尚未安裝）
# Linux:
curl -fsSL https://ollama.com/install.sh | sh

# 啟動 Ollama 服務
ollama serve

# 在另一個終端下載模型（首次使用需下載）
ollama pull llama3.2
```

### 4.2 啟動後端

```bash
source .venv/bin/activate
cd backend
uvicorn main:app --host 127.0.0.1 --port 8088 --reload
```

### 4.3 開啟前端

瀏覽器打開：**http://127.0.0.1:8088**

前端頁面由 FastAPI 靜態文件掛載自動提供，無需額外啟動。

---

## 5. 前端使用指南

### 5.1 首頁

進入首頁後，你會看到兩個主要功能按鈕：

- **🍜 附近吃什麼？** — 美食推薦
- **🎯 附近去哪玩？** — 景點推薦
- **👤 個人資料** — 查看 Helen 的用戶畫像

### 5.2 位置設定

應用會嘗試取得你的真實定位。如果不在紐約，可以切換到 **🧪 測試模式**，從 8 個預設位置中選擇：

| 位置 | 說明 |
|------|------|
| Times Square | 時代廣場（曼哈頓中城） |
| Central Park | 中央公園 |
| Brooklyn Bridge | 布魯克林大橋 |
| Flushing | 法拉盛（亞洲美食天堂） |
| Chinatown | 曼哈頓唐人街 |
| Williamsburg | 威廉斯堡（文青區） |
| SoHo | 蘇活區（購物藝術） |
| Upper East Side | 上東區（博物館一條街） |

### 5.3 搜尋半徑與模式

頁面頂部有 **Pipeline 設定面板**：

- **搜尋半徑**：0.5 ~ 20 km，拖拉滑桿調整
- **AI 模式**：LLM 智慧排序（需等待約 10-30 秒）
- **Raw 模式**：純數據排序（按評論數 Top 50，毫秒回應）

### 5.4 美食推薦流程

1. 點擊「附近吃什麼？」
2. **AI 模式**：填寫問卷（菜系、預算、口味偏好等，可多選），送出後等待 AI 排序
3. **Raw 模式**：可選擇類型篩選（如 ramen_restaurant），點擊搜尋按鈕即可
4. 結果頁顯示排名卡片，每張卡包含：評分、距離、描述、AI 推薦理由、詳細評語

### 5.5 景點推薦流程

1. 點擊「附近去哪玩？」
2. 選擇類別：🏛 文化 | 🎭 娛樂 | 🛍 購物 | 🌿 其他
3. **AI 模式**：填寫問卷→等待 AI 排序
4. **Raw 模式**：選類別→選類型篩選→搜尋

### 5.6 結果頁面

每張推薦卡片包含：

| 欄位 | 說明 |
|------|------|
| 排名 | AI 排序名次（AI 模式）或評論數排名（Raw 模式） |
| 名稱 | 地點名稱 |
| 評分 | Google 評分 ⭐ |
| 距離 | 與你的距離（公尺） |
| 描述 | Google 編輯摘要 |
| 推薦理由 | AI 生成的個人化推薦語 |
| 詳細評語 | AI 綜合評價（4-6 句話） |
| 亮點標籤 | 如「性價比高」「適合拍照」 |
| 警告標籤 | 如「高峰期排隊」 |
| 資料來源 | 📍Google / 🤖AI / 🔍差評 / 📰編輯推薦 |

---

## 6. API 說明

所有 API 前綴為 `/api`。

### 健康檢查

```
GET /api/health
→ {"status": "ok"}
```

### LLM 狀態

```
GET /api/llm-provider
→ {"provider": "ollama", "model": "llama3.2", "base_url": "http://localhost:11434"}
```

### 用戶畫像

```
GET /api/profile
→ {name, summary, structured, food_likes, food_dislikes, ...}
```

> 用戶畫像已脫敏，不含真實姓名、學校等隱私資訊。

### 標籤列表

```
GET /api/labels?data_source=manhattan&category=food_drink
→ {"labels": [{"label": "ramen_restaurant", "count": 45}, ...]}
```

### 美食推薦

```
POST /api/eat
Content-Type: application/json

{
  "latitude": 40.758,
  "longitude": -73.9855,
  "radius_km": 1.0,
  "data_source": "manhattan",
  "mode": "ai",
  "questionnaire": { ... }
}
```

### 景點推薦

```
POST /api/explore
Content-Type: application/json

{
  "latitude": 40.758,
  "longitude": -73.9855,
  "radius_km": 2.0,
  "data_source": "manhattan",
  "category": "culture",
  "mode": "ai",
  "questionnaire": { ... }
}
```

### Swagger 文件

啟動伺服器後造訪 **http://127.0.0.1:8088/docs** 可查看完整互動式 API 文件。

---

## 7. AI 模式 vs Raw 模式

| | AI 模式 | Raw 模式 |
|---|---|---|
| **排序方式** | LLM 綜合排序（考慮偏好、評價、差評、編輯推薦） | 按 Google 評論數量排序 |
| **回應時間** | 10-30 秒 | < 100 毫秒 |
| **需要 LLM** | ✅ 需要 Ollama 或 Azure OpenAI | ❌ 不需要 |
| **個人化** | ✅ 根據問卷與用戶畫像 | ❌ 純數據 |
| **回傳數量** | Top 25 | Top 50 |
| **適用場景** | 深度推薦 | 快速瀏覽附近熱門地點 |

> 💡 如果 LLM 沒有配置或連線失敗，AI 模式會自動降級為按評論數排序。

---

## 8. 專案結構

```
nyc-travel-assistant/
├── backend/
│   ├── main.py              # FastAPI 入口 + 路由掛載
│   ├── config.py            # 所有設定（DB路徑、LLM、CORS）
│   ├── requirements.txt     # Python 依賴
│   ├── .env                 # 環境變數（需自行建立）
│   ├── api/
│   │   ├── eat.py           # POST /api/eat
│   │   ├── explore.py       # POST /api/explore
│   │   ├── profile.py       # GET /api/profile
│   │   └── models.py        # 請求/回應模型定義
│   └── services/
│       ├── pipeline.py      # 推薦 Pipeline 核心邏輯
│       ├── llm.py           # LLM 呼叫（Azure / Ollama）
│       ├── geo.py           # 地理篩選 + Haversine 距離
│       ├── web_search.py    # DuckDuckGo 差評搜尋
│       └── profile.py       # 用戶畫像解析
├── frontend/
│   ├── index.html           # SPA 單頁入口
│   ├── css/style.css        # 樣式（紫色主題 + 動畫）
│   ├── js/
│   │   ├── app.js           # 路由 + 頁面渲染
│   │   ├── config.js        # 前端設定 + 問卷題目
│   │   ├── api.js           # API 請求 + 定位服務
│   │   ├── questionnaire.js # 問卷組件
│   │   └── samoyed.js       # 薩摩耶動畫
│   └── img/                 # 圖片素材
├── dataset/
│   ├── manhattan/classifications/   # 曼哈頓 SQLite DB
│   ├── brooklyn/classifications/    # 布魯克林 SQLite DB
│   └── online_recommendations/      # 線上編輯推薦 DB
├── user_profil/
│   └── helen_profile.md    # Helen 的完整用戶畫像
└── SPEC.md                  # 完整技術規格文件
```

### Pipeline 流程圖

```
用戶送出問卷
    ↓
① 冷篩（cold_filter）
   → 半徑內的地點，按評論數取 Top 25
    ↓
② 差評搜尋（web_search）
   → DuckDuckGo 搜尋每個地點的負面評價
    ↓
③ 線上推薦（online_recommendations）
   → 從編輯推薦 DB 查詢相關推薦
    ↓
④ LLM 排序（call_llm）
   → 將用戶畫像 + 問卷 + 全部數據送給 LLM
   → LLM 回傳 JSON 格式的智慧排名
    ↓
⑤ 合併與回傳
   → 結構化回應 → 前端渲染結果卡片
```

---

## 9. 常見問題

### Q: 沒有 LLM 也能用嗎？

可以。切換到 **Raw 模式** 就不需要 LLM，直接按評論數排序顯示附近的地點。

### Q: Ollama 該用哪個模型？

| 模型 | 記憶體需求 | 中文能力 | 推薦度 |
|------|-----------|----------|--------|
| `llama3.2` | ~4GB | 一般 | ⭐⭐⭐ |
| `qwen2.5:7b` | ~5GB | 好 | ⭐⭐⭐⭐ |
| `qwen2.5:14b` | ~10GB | 很好 | ⭐⭐⭐⭐⭐ |

```bash
# 下載模型
ollama pull qwen2.5:14b

# 在 .env 裡改模型
OLLAMA_MODEL=qwen2.5:14b
```

### Q: 怎麼切換 Azure 和 Ollama？

修改 `.env` 裡的 `LLM_PROVIDER`：

```env
# 用 Ollama
LLM_PROVIDER=ollama

# 用 Azure OpenAI
LLM_PROVIDER=azure
```

重啟伺服器後生效。可以用 `GET /api/llm-provider` 確認當前使用的 LLM。

### Q: AI 模式回應很慢？

- Ollama 本地推理速度取決於硬體（GPU 加速會快很多）
- Azure OpenAI 通常 10-15 秒
- 如果只是快速瀏覽，用 Raw 模式即可

### Q: 不在紐約怎麼測試？

點擊首頁的 **🧪 測試模式** 按鈕，選擇一個預設的紐約位置即可。

### Q: 資料庫裡有多少地點？

- **曼哈頓**：約 1,190 個地點
- **布魯克林**：約 1,158 個地點
- **線上推薦**：額外的編輯精選推薦

### Q: 前端需要額外打包嗎？

不需要。前端是純 HTML + CSS + JS，由 FastAPI 的 `StaticFiles` 直接提供，開箱即用。
